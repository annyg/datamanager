% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert_mapping_to_dictionary.R
\name{convert_mapping_to_dataframe}
\alias{convert_mapping_to_dataframe}
\title{Convert a textual mapping definition into a long-format data frame}
\usage{
convert_mapping_to_dataframe(mapping_text)
}
\arguments{
\item{mapping_text}{Character. The full mapping text (a single string,
typically read from a file).}
}
\value{
A data frame with columns \code{Variable}, \code{Label}, and
\code{Value_Label}. Rows representing the variable header have \code{NA} in
\code{Value_Label}; rows representing value labels carry the value in
\code{Value_Label} and the value's label in \code{Label}.
}
\description{
Parses a multi-line mapping string of the form used in this package's
custom variable definition syntax (variable headers like
\code{Var_name : 'Variable label' <- TYPE :: oldname} followed by indented
value rows like \code{1 = 'Yes' <- 1}) and produces a long-format data frame
with one row per variable and one row per value label.
}
\details{
Generally \code{\link[=parse_variable_mapping]{parse_variable_mapping()}} (which reads from a file path and
returns a wide format with one row per variable) is preferred; this
helper exists primarily for inline strings.
}
\seealso{
\code{\link[=convert_mapping_to_dictionary]{convert_mapping_to_dictionary()}} for a list-shaped result,
and \code{\link[=parse_variable_mapping]{parse_variable_mapping()}} for the file-based parser.
}
