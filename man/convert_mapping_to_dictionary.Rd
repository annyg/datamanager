% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert_mapping_to_dictionary.R
\name{convert_mapping_to_dictionary}
\alias{convert_mapping_to_dictionary}
\title{Convert a textual mapping definition into a nested dictionary}
\usage{
convert_mapping_to_dictionary(mapping_text)
}
\arguments{
\item{mapping_text}{Character. The full mapping text (a single string,
typically read from a file).}
}
\value{
A named list. Each element corresponds to one variable and has
the structure \code{list(label = "...", values = list("0" = "No", "1" = "Yes", ...))}.
}
\description{
Parses the same mapping syntax as \code{\link[=convert_mapping_to_dataframe]{convert_mapping_to_dataframe()}} but
returns a nested list keyed by variable name, with each element holding
the variable's \code{label} and a named list of \code{values} (value → value
label).
}
\seealso{
\code{\link[=convert_mapping_to_dataframe]{convert_mapping_to_dataframe()}},
\code{\link[=parse_variable_mapping]{parse_variable_mapping()}}.
}
